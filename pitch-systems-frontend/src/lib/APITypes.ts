//STUBS

export type AudioAnalysis = {
    freqs: number[],

}

export type Interval = {
    id: number,
    name: string,
    cents: number
}

export type Scale = {
    id: number,
    name: string
}